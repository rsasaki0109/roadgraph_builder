"""Road graph construction from multi-modal sensor data (MVP: trajectory)."""

__version__ = "0.1.0"

# Future: LiDAR-based boundary extraction, camera semantics, graph fusion,
# intersection topology estimation, routing graph generation.